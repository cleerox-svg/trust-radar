# Frontend built assets — generated by CI before wrangler deploy
public/v2/
public/tenant/
# Astro marketing build output (R1+) — written by @averrow/marketing's
# build step. Source lives in packages/averrow-marketing/.
public/marketing/
