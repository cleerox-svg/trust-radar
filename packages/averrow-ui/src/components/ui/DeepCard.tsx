// Averrow Design System — DeepCard (alias for Card)
// DeepCard is now an alias for Card. Import Card directly for new code.

export { Card as DeepCard } from './Card';
export type { CardProps as DeepCardProps, CardVariant as DeepVariant } from './Card';
