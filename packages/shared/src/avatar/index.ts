// @averrow/shared/avatar — canonical initials + color helpers.
// Per SHARED_LOGIN_SPEC §3 avatars are initials-only across all
// products; never the Google profile picture.

export { parseInitials, colorForUserId, SELF_AVATAR_COLOR } from './avatar';
