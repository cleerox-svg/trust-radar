-- Migration: 0015_expand_platform_constraint
-- Originally: Recreated monitored_accounts table with expanded platform CHECK constraint.
-- This change already exists in the remote DB (applied before migration tracking).
-- No-op marker so the migration tracker advances past it.

SELECT 1;
